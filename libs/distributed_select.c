#include <mpi.h>

// on request send random number in range
// on request send number gt and number lt
void worker() {}

// while true
// pick random and request random in range
// request from all the number less than and greater than
// summ lt and gt if equal return answer else reduce the range and repeat
void central() {}
